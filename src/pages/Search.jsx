import React from 'react';
import { ProductResults } from '../components/ProductsResults';

export const Search = ({}) => {
	return (
		<div className='' id='search'>
			<ProductResults />
		</div>
	);
};
